{
  "name": "server-rest",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "PORT=3001 nodemon ./bin/www",
    "tester": "NODE_ENV=test mocha \"routes/*.test.js\"",
    "test": "concurrently --success first -k \"NODE_ENV=test PORT=3004 node ./bin/www\" \"yarn tester\""
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "morgan": "~1.9.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "concurrently": "^4.1.0",
    "mocha": "^5.2.0",
    "supertest": "^3.4.2"
  }
}
